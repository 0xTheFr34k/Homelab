---
# 2. Istiod
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istiod
  namespace: flux-system
spec:
  releaseName: istiod
  targetNamespace: istio-system
  interval: 1m
  chart:
    spec:
      chart: istiod
      version: 1.27.1
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: flux-system
